name: Deploy Pulse Publisher

on:
  push:
    branches:
      - deploy-dev
      - deploy-exp
      - deploy-prod
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - exp
          - prod

# Prevent concurrent deployments per environment
concurrency:
  group: pulse-deployment-${{ github.event_name == 'workflow_dispatch' && github.event.inputs.environment || github.ref_name }}
  cancel-in-progress: false # This will apparently be queued if there is a collision

permissions:
  id-token: write # Required for OIDC authentication with AWS
  contents: read

jobs:
  # Job 1: Build and test pulse-publisher
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set target environment
        id: set-env
        run: |
          # For manual dispatch, use the selected environment; for branch push, extract from branch name
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "environment=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
          else
            # Extract environment from branch name (deploy-dev -> dev, deploy-exp -> exp, deploy-prod -> prod)
            BRANCH_NAME="${{ github.ref_name }}"
            ENV="${BRANCH_NAME#deploy-}"
            echo "environment=$ENV" >> $GITHUB_OUTPUT
            echo "Deploying to environment: $ENV (from branch: $BRANCH_NAME)"
          fi

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build pulse-publisher
        run: pnpm --filter pulse-publisher... build

      - name: Test pulse-publisher
        run: pnpm --filter pulse-publisher test

      - name: Upload build artefacts
        uses: actions/upload-artifact@v4
        with:
          name: pulse-build-artefacts
          path: apps/pulse-publisher/dist/
          retention-days: 7

  # Job 2: Deploy pulse-publisher
  # This job calls the reusable deployment workflow
  # AWS credentials are passed explicitly from repository secrets
  # Secrets are selected based on the target environment
  deploy:
    needs: build-and-test
    uses: ./.github/workflows/reusable-deploy-lambda.yml
    with:
      app-name: pulse-publisher
      environment: ${{ needs.build-and-test.outputs.environment }}
      working-directory: apps/pulse-publisher
      output-key: PulsePublisherFunction
      artefact-name: pulse-build-artefacts
      policy-file: .github/policies/pulse-publisher-deploy-policy.json
    secrets:
      aws-oidc-role-arn: ${{
        (needs.build-and-test.outputs.environment == 'dev' && secrets.AWS_OIDC_ROLE_ARN__MONOREPO_FEM__PULSE_PUBLISHER__DEV) ||
        (needs.build-and-test.outputs.environment == 'exp' && secrets.AWS_OIDC_ROLE_ARN__MONOREPO_FEM__PULSE_PUBLISHER__EXP) ||
        (needs.build-and-test.outputs.environment == 'prod' && secrets.AWS_OIDC_ROLE_ARN__MONOREPO_FEM__PULSE_PUBLISHER__PROD)
      }}
