{
  "name": "heartbeat-publisher",
  "version": "0.0.1",
  "private": true,
  "description": "AWS Lambda function that publishes heartbeat logs to CloudWatch",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "clean": "rm -rf dist/ .aws-sam/",
    "lint": "echo 'Linting not yet configured'",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "format": "echo 'Formatting not yet configured'",
    "check": "echo 'Check not yet configured'"
  },
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "dependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3.921.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^24.9.2",
    "@vitest/coverage-v8": "^4.0.6",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "^0.25.11",
    "typescript": "^5.7.3",
    "vitest": "^4.0.6"
  }
}
